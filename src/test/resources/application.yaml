spring:
  config:
    activate:
      on-profile: test

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

logging:
  level:
    root: INFO
    org.springframework.jdbc.core: DEBUG # JDBC 쿼리 로그
    com.team5.studygroup: DEBUG # 여기만 실제 패키지로 수정!